import { TProcessCode } from 'types/navigation';

// 4000 - 4999 @last
const CONFIG_PROCESS_CODES: TProcessCode = {
  READING_FILE_FAILED: 4000,
  FILE_READ: 4001,
};

export default CONFIG_PROCESS_CODES;
