export type TdbCollection = {
  users: {
    name: string;
    label: string;
  };
  schools: {
    name: string;
    label: string;
  };
  programs: {
    name: string;
    label: string;
  };
  courses: {
    name: string;
    label: string;
  };
  units: {
    name: string;
    label: string;
  };
  topics: {
    name: string;
    label: string;
  };
  media_content: {
    name: string;
    label: string;
  };
};
