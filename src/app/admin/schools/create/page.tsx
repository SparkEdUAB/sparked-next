'use client';

import CreateSchoolView from '@components/school/createSchoolView';
import React from 'react';

const CreateSchool: React.FC = (props) => {
  return <CreateSchoolView />;
};

export default CreateSchool;
